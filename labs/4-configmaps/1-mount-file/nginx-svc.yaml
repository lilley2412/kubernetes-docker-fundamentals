apiVersion: v1
kind: Service
metadata:
  name: lab4-1
  namespace: default
  labels:
    name: lab4-1
    lab: lab4-1
spec:
  type: ClusterIP
  selector:
    lab: lab4-1
  ports:
  - port: 80
    targetPort: 80
